const Utilitario = require('../lib/Utilitario');
describe('Utilitario', () => {
      test ('Vetor nullo',() => {
  expect(num = null);

 // test('vetor com zero elementos',()=>{
//expect(num1 == 0);
  //});
   
  
 
    });
});